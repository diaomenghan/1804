1、vim /etc/passwd,usermod -s, useradd -s

2、$USER 当前登陆用户	$RANDOM 随机数	$HOME 家目录	$SHELL 目前使用的解释器
$HOSTNAME 主机名	$PS1 Shell环境的一级提示符	$PS2 Shell环境的二级提示符

3、 &>  >/dev/null

4、#!/bin/bash
read -p "请输入第一个数字：" i
read -p "请输入第二个数字：" j
read -p "请输入第三个数字：" k
if [ $i -ge $j ];then
	if [ $k -ge $i ];then
		echo $k $i $j
	elif [ $k -ge $j ];then
		echo $i $k $j
	else
		echo $i $j $k
	fi
else 
	if [ $k -ge $j ];then
		echo $k $j $i
	elif [ $k -ge $i ];then	
		echo $j $k $i
	else
		echo $j $i $k
	fi
fi

6、#!/bin/bash
read -p "输入剪刀、石头、布之一：" haha
pc=$[RANDOM%3]
case $haha in
剪刀)
	case $pc in
	0)
		echo 平局;;
	1)
		echo 输了;;
	2)	
		echo 赢了
	esac;;
石头)
	case $pc in
        0)
                echo 赢了;;
        1)
                echo 平局;;
        2)
                echo 输了
        esac;;
布)
	case $pc in
        0)
                echo 输了;;
        1)
                echo 赢了;;
        2)
                echo 平局
        esac
esac

7、#!/bin/bash
for i in `seq 254`
do
	ping  -i0.1 -W1 -c2 192.168.4.$i &> /dev/null && echo 4.$i up || echo 4.$i down
done

8、#!/bin/bash
for i in {1..9}
do
	for j in `seq $i`
	do
		echo -n "$i*$j=$[i*j]	"
	done
	echo
done

9、#!/bin/bash
grep "GenuineIntel" /proc/cpuinfo &> /dev/null
[ $? -eq 0 ] && echo cpu为intel公司 || cpu cpu为AMD公司

10、#!/bin/bash
while :
do
	ifconfig p8p1 | grep "[RT]X packets"
done

11、#!/bin/bash
for i in `cat user.txt`
do
	useradd $i
	echo 123456 | passwd --stdin $i
done


12、#!/bin/bash
for i  in `ls *.$1`
do
	mv $i ${i%.$1}.$2
done

13、#!/bin/bash
expect <<EOF
spawn ssh root@172.25.0.11
expect "password"	{ send "redhat\r"}
expect "#"	 	{ send "yum -y install httpd \r"}
expect "#"	 	{ send "exit\r"}
EOF








